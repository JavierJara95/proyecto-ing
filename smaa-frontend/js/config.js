// Configuracion opcional para generar QR accesibles desde celulares en la misma red WiFi.
// Si el sistema se abre en el PC con http://localhost, el QR necesita una URL con la IP del PC.
// Ejemplo manual:
// window.SMAA_PUBLIC_BASE_URL = 'http://192.168.1.25';
window.SMAA_PUBLIC_BASE_URL = window.SMAA_PUBLIC_BASE_URL || '';
